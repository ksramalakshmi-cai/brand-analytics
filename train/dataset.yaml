# YOLO Dataset Configuration Template
# -----------------------------------------------
# Auto-generated by train/prepare_dataset.py
# Edit this file manually if needed, then run:
#   python train/train.py --data train/dataset.yaml

# Absolute path to dataset root (auto-set by prepare_dataset.py)
path: train/data

# Relative paths from 'path' above
train: images/train
val:   images/val
# test: images/test   # optional

# Number of classes
nc: 13

# Class names — must match your labels.txt order exactly
# Update this list after running prepare_dataset.py
names:
  - google
  - coca_cola
  - budweiser
  - toyota
  - hyundai
  - emirates
  - aramco
  - dp_world
  - sobha
  - flipkart
  - royal_stag
  - apollo
  - livpure
